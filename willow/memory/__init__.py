"""
willow.memory — stolen and adapted from moonshine + adjoint.

Modules:
  generation  — generation-counter fact store wrapping SOIL writes
                (stolen from moonshine's observer/reflect.js generation concept)
  norn_hook   — PII-scrubbing pre-extraction hook
                (stolen from adjoint's redact.py + Norn pattern)
"""
