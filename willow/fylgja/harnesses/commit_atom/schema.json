{
  "type": "object",
  "properties": {
    "title": {"type": "string", "maxLength": 80},
    "summary": {"type": "string", "maxLength": 600},
    "category": {
      "type": "string",
      "enum": ["feature", "bugfix", "refactor", "test", "docs", "infra"]
    },
    "files_touched": {
      "type": "array",
      "items": {"type": "string"},
      "maxItems": 20
    },
    "breaking_or_risky": {"type": "boolean"},
    "risk_note": {"type": "string", "maxLength": 200}
  },
  "required": [
    "title", "summary", "category", "files_touched",
    "breaking_or_risky", "risk_note"
  ],
  "additionalProperties": false
}
