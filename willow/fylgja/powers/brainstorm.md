@markdownai v1.0

# power: brainstorm
b17: FYLP1 · ΔΣ=42

**When:** New behavior, new surface area, or ambiguous goal.

1. `kb_search` + `soil_search` (`{agent}/atoms` or domain [user] uses) — one query each max unless empty.
2. Problem in **one sentence**.
3. **Three** approaches — name + one tradeoff line each.
4. Pick one — **two sentences** why.
5. Fylgja touch? Hooks/MCP/install.py → note constraint; don’t design around bypass.
6. **Stop** until [user] ratifies (explicit OK).

**Don’t:** Implement, scaffold, or “quick tweak” before step 6.
