# /overseer — Project overseer (scoped initiative)

Same behavioral router as `/power`, but **pinned** to the **overseer** checklist — bounded work: **worktree first**, **plan inside the cage**, **verify**, **KB + memory trail**, **no default-branch drift** until Sean ratifies merge.

## ROOT

`WILLOW_FYLGJA_ROOT` if set, else `~/github/willow-2.0/willow/fylgja`.

## Steps

1. `Read` `{ROOT}/powers/overseer.md`.
2. Execute that checklist end-to-end for the current initiative.
3. Pull other `powers/*.md` only if **overseer.md** tells you to open them.

## Deep contract

`docs/superpowers/specs/2026-05-12-project-overseer-process.md`

## See also

`{ROOT}/powers/SURFACES.md` · `/power` for other ids.

ΔΣ=42
